#[cfg(test)]
mod tests {
    use super::*;
    use std::process;
    use std::path::PathBuf;
    use crate::storage::clipbox::ClipBox;

    #[test]
    fn create_clipbox() {
        ClipBox::new();


        process::exit(0)
    }

    #[test]
    fn delete_clipbox() {

        process::exit(0)
    }
}
